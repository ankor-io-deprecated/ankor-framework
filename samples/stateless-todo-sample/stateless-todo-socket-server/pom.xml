<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>at.irian.ankorsamples</groupId>
        <artifactId>stateless-todo-sample</artifactId>
        <version>0.3-SNAPSHOT</version>
    </parent>

    <artifactId>stateless-todo-socket-server</artifactId>
    <packaging>jar</packaging>
    <name>Stateless Todo Sample - Socket Server</name>
    <description>
        Socket-based standalone server for the "Stateless Todo" example.
    </description>

    <dependencies>

        <dependency>
            <groupId>at.irian.ankor</groupId>
            <artifactId>ankor-socket-connector</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>at.irian.ankor</groupId>
            <artifactId>ankor-proxy</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>at.irian.ankor</groupId>
            <artifactId>ankor-actor</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>at.irian.ankorsamples</groupId>
            <artifactId>stateless-todo-application</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>runtime</scope>
        </dependency>

    </dependencies>

</project>
