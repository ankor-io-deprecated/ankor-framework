
- RMA durch abstraktere Action ersetzen
  * Idee: Konvention   action="saveAnimal"  findet autom. annotated methode beim Bean
  * eigenes modul "ankor-annotation"



  @AnkorAction(actionName="save", actionPropertyType=AnimalDetailModel.class)
  public void saveAnimal(
    @AnkorActionPropertyRef Ref modelRef,
    @AnkorActionParam("tabId") String tabId,
    @AnkorActionProperty("animal") Animal animal)


- Konzept "model context" auflassen

- model zugriffe (oder listener aufrufe) synchronized machen?

- basis klasse AbstractAnkorSystem

- multi threaded test server

- JSF client

- Servlet container server (HTTP, user session)

- getrennte Instanzen server / client

- messages mit task executor abarbeiten

- Resource saving / loading ("Tierbilder")
